{
  "name": "@tools/lerna",
  "private": true,
  "scripts": {
    "start": "lerna run start:dev --parallel --scope @tools/web --scope @tools/server"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "cuid": "^2.1.8"
  }
}
